# Zippy Admin Web

Minimal operational dashboard for the FastAPI backend.

## Commands

```powershell
cd admin-web
npm run generate:api
npm test
```

Open `index.html` directly in a browser, or serve this folder with any static file server. The dashboard reads the backend from `http://127.0.0.1:8000` by default; change the API base URL in the header input.
